import UserProfilePage from '@/user/Profile'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
      <div>
          <UserProfilePage />
    </div>
  )
}

export default page