import RegistrationPage from '@/auth/Register'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
      <div>
          <RegistrationPage />
    </div>
  )
}

export default page