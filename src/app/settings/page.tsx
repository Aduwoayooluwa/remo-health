import SettingsPage from '@/doctor/Settings'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
      <div>
          <SettingsPage />
    </div>
  )
}

export default page